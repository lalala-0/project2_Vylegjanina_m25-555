# Примитивная база данных

**Примитивная база данных** - простая консольная СУБД с хранением структуры таблиц в JSON. Позволяет создавать и удалять таблицы, описывать их схему, просматривать список таюлиц через интерактивный терминал.

---

## Возможности

- Создание таблиц с указанием типа столбцов
- Удаление таблиц
- Получение списка всех таблиц
- Проверка корректности типов столбцов
- Загрузка метаданных и их сохранение при их изменении
- Поддержка CRUD-операций (insert, select, update, delete)
- Автоматическая обработка ошибок, логирование времени выполнения и кеширование повторных запросов

Поддерживаемые типы: `int`, `str`, `bool`

---

## 🛠 Установка

> Убедитесь, что у вас установлен Python 3.11+ и, если используете второй способ установки, менеджер зависимостей Poetry.

### Вариант 1: через Poetry

```sh
poetry install
```

### Вариант 2: через Makefile

```sh
make install
```
---

## 🚀 Запуск

>PrimitiveDB сохраняет метаданные таблиц в файл `data/metadata.json`.
Путь фиксированный. Если в корневом каталоге проекта нет папки `data`, сохранение выполнено не будет.

### Через Poetry
```sh
poetry run database
```
### Через Makefile

```sh
make project
```
---

## 📦 Установка и запуск как пакет

> Публикация выполняется **локально**, без выгрузки на PyPI. Поэтому, помимо установки и запуска пакета, ниже описана его сборка.

### 1️⃣ Сборка пакета

```sh
make build
```
После этого в каталоге `dist/` появится файл вида `dist/project2_vylegjanina_m25_555-0.1.0-py3-none-any.whl`. 
> Если в будущем будет публикация в PyPI, этот шаг можно будет пропустить.

### 2️⃣ Установка собранного пакета

```sh
make package-install
```

### 3️⃣ Запуск установленного пакета

После успешной установки команда `database` будет доступна напрямую:

```sh
database
```

---

## 📌 Команды

### Управление таблицами
> Имена таблиц и столбцов чувствительны к регистру.

| Команда | Пример | Описание |
| ------- | ------ | -------- |
| `create_table <name> <col:type> <col:type>` | `create_table users name:str active:bool` | Создать таблицу        |
| `drop_table <name>`                         | `drop_table users`                        | Удалить таблицу        |
| `list_tables`                               | `list_tables`                             | Вывести список таблиц  |
| `exit`                                      | `exit`                                    | Выйти из программы     |
| `help`                                      | `help`                                    | Вывести описание допустимых команд |
### CRUD-операции
> Строки вводятся в кавычках, булевые - `true/false`, целые числа - без кавычек.

| Команда | Пример | Описание | Дополнительное описание |
| ------- | ------ | -------- | ----------- |
| `insert into <имя_таблицы> values (<значение1>, <значение2>, …)` | `insert into users values ("Alice", true, 25)` | Создать новую запись в таблице | Значения передаются строго в порядке, указанном в схеме.  |
| `select from <имя_таблицы>` | `select from users` | Прочитать все записи из таблицы | Возвращает все строки таблицы. Если данных нет - выводится сообщение «Нет данных по запросу». |
| `select from <имя_таблицы> where <столбец> = <значение>` | `select from users where active = true` | Возвращает записи таблицы, подходящие по условию | Поддерживаются только операторы равенства (`=`). Тип значения должен соответствовать типу столбца. Если данных нет - выводится сообщение «Нет данных по запросу».|
| `update <имя_таблицы> set <столбец1> = <новое_значение1> where <столбец_условия> = <значение_условия>` | `update users set active = false where name = "Alice"` | Обновить запись по условию | Можно обновлять несколько столбцов сразу. Типы значений соответствуют типам, заявленным в схеме. По завершении операции выводится количество обновленных строк. |
| `delete from <имя_таблицы> where <столбец> = <значение>` | `delete from users where ID = 1` | Удалить запись по условию | По окончании операции выводит количество удаленных записей |
| `info <имя_таблицы>` | `info users` | Показать схему таблицы и количество записей | Выводит имена столбцов, их типы и общее количество записей в таблице. |

### Подтверждение опасных действий
Перед удалением таблицы или записей система запрашивает подтверждение:
`Вы уверены, что хотите выполнить "удаление таблицы"? [y/n]:`
Если ввести y, операция выполняется. Любой другой ввод отменяет действие.

### Централизованная обработка ошибок
При неверных командах, несуществующих таблицах или ошибках типов программа выводит понятное сообщение (например: “таблица не найдена”, “ошибка валидации”) и продолжает работу - без аварийного завершения.

### Информирование о времени выполнения
Для длительных операций (например, insert или select) отображается время выполнения, например:
`Функция select выполнилась за 0.123 секунд.`

### Кэширование результатов SELECT
Повторные одинаковые запросы select выполняются быстрее - результаты сохраняются в памяти на время текущего запуска программы. 
После перезапуска кэш очищается.

---

## 🎥 Демонстрация

### Опреации с таблицами
[![Опреации с таблицами](https://asciinema.org/a/AzKsbusRqQqpgYuuMxSGe2HBQ.svg)](https://asciinema.org/a/AzKsbusRqQqpgYuuMxSGe2HBQ)
### CRUD опреации
[![CRUD опреации](https://asciinema.org/a/F3oaZtafSMGiqoltFYkEIGnpt.svg)](https://asciinema.org/a/F3oaZtafSMGiqoltFYkEIGnpt)
### Демонстрация работы декораторов
[![](https://asciinema.org/a/jqPQae624DZOATgxpGKMgTfCo.svg)](https://asciinema.org/a/jqPQae624DZOATgxpGKMgTfCo)

---

## 📂 Структура проекта

```
src
  decorators.py    # декораторы
  primitive_db  
    core.py        # операции с таблицами, CRUD
    utils.py       # загрузка/сохранение JSON
    parser.py      # разбор команд
    engine.py      # основной цикл программы
    main.py        # точка входа
```

---

## Статус проекта

* Поддерживает интерактивный режим
* Реализованы CRUD-операции
* Метаданные и данные сохраняются в JSON
* Поддержка проверки типов при вставке и обновлении

---

## Автор

Алёна Вылегжанина, М25-555
